import 'package:flutter/material.dart';

class AppColors {
  /// Primary brand color (same as Login / Register)
  static const primary = Color(0xFFE56C7A);

  /// Soft background (cream)
  static const background = Color(0xFFFFFBF7);

  /// Cards / surfaces
  static const surface = Colors.white;

  /// Text
  static const textPrimary = Color(0xFF1F1F1F);
  static const textSecondary = Color(0xFF8E8E93);

  /// Pastel accents (optional use)
  static const pinkSoft = Color.fromARGB(255, 255, 192, 198);
  static const peachSoft = Color.fromARGB(255, 255, 200, 156);
  static const mintSoft = Color.fromARGB(255, 178, 255, 225);
  static const lavenderSoft = Color.fromARGB(255, 202, 185, 255);
  static const blueSoft = Color.fromARGB(255, 188, 228, 255);
}
